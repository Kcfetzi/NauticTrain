using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class UISubmitCancelPopup : UIPopup
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void Init(string title, string text, string submitButtonText, string cancelButtonText, UnityAction submitCallback, UnityAction cancelCallback)
    {
        throw new System.NotImplementedException();
    }
}
